# mantella-xtts-skyrim
XTTS (Mantella) server with Skyrim NPC voice latents baked in (en + ru + pt).
Built & pushed to `ghcr.io/<owner>/mantella-xtts-skyrim:latest` by GitHub Actions.
Synthesize via `POST /tts_to_audio/` with `speaker_wav=<voicename>` (e.g. femalenord, astrid) and `language=ru`.
